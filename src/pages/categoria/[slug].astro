---
import MainLayout from '../../layouts/MainLayout.astro';
import { getCategoryBySlug, getAllCategorySlugs, categories } from '../../data/categories';
import { generateArticleSchema } from '../../lib/seo';

export async function getStaticPaths() {
  return getAllCategorySlugs().map(slug => ({
    params: { slug },
  }));
}

const { slug } = Astro.params;
const category = getCategoryBySlug(slug!);

if (!category) {
  return Astro.redirect('/404');
}

const breadcrumb = [
  { name: 'Inicio', url: 'https://cosmeticoscoreanos.co' },
  { name: 'Categorías', url: 'https://cosmeticoscoreanos.co/categorias' },
  { name: category.name, url: `https://cosmeticoscoreanos.co/categoria/${slug}` },
];

// Related categories
const relatedCategories = categories.filter(c => c.slug !== slug).slice(0, 4);

const articleSchema = generateArticleSchema({
  headline: `${category.name} | K-Beauty Colombia`,
  description: category.metaDescription,
  image: category.image,
  datePublished: '2026-01-01',
  author: 'Cosméticos Coreanos Colombia',
  url: `https://cosmeticoscoreanos.co/categoria/${slug}`,
});
---

<MainLayout 
  title={`${category.name} | Cosméticos Coreanos Colombia`}
  description={category.metaDescription}
  image={category.image}
  breadcrumb={breadcrumb}
  schema={articleSchema}
>
  <div class="container">
    <nav class="breadcrumb">
      <a href="/">Inicio</a>
      <span>/</span>
      <a href="/categorias">Categorías</a>
      <span>/</span>
      <span class="current">{category.name}</span>
    </nav>

    <article class="category-page">
      <header class="category-header">
        <h1>{category.name}</h1>
        <p class="category-description">{category.description}</p>
        
        <div class="category-meta">
          <div class="meta-item">
            <span class="meta-label">Características</span>
            <div class="meta-tags">
              {category.features.map(feature => (
                <span class="tag">{feature}</span>
              ))}
            </div>
          </div>
        </div>
      </header>

      <section class="content-section">
        <h2>¿Qué es {category.name}?</h2>
        <p>Los {category.name.toLowerCase()} son productos esenciales en la rutina de skincare coreana. Conocidos por su formulación innovadora y efectividad, estos productos han ganado popularidad mundial en el mundo del K-Beauty.</p>
        
        <h3>Beneficios principales</h3>
        <ul class="benefits-list">
          {category.features.map(feature => (
            <li>{feature}</li>
          ))}
        </ul>

        <h3>Guía de elección</h3>
        <p>Al elegir un {category.name.toLowerCase()}, considera tu tipo de piel:</p>
        <ul>
          <li><strong>Piel seca:</strong> Busca productos con ácido hialurónico y ceramidas</li>
          <li><strong>Piel mixta:</strong> Texturas ligeras que no obstruyan los poros</li>
          <li><strong>Piel grasosa:</strong> Productos oil-free y no comedogénicos</li>
          <li><strong>Piel sensible:</strong> Ingredientes calmantes como centella asiatica</li>
        </ul>
      </section>

      <section class="content-section">
        <h2>Productos Recomendados</h2>
        <p>Explora nuestra selección de los mejores {category.name.toLowerCase()} disponibles en Colombia.</p>
        
        <div class="products-grid">
          <p class="coming-soon">Próximamente: Lista de productos detallados con reseñas y comparaciones.</p>
        </div>
      </section>

      <section class="faq-section">
        <h2>Preguntas Frecuentes sobre {category.name}</h2>
        <div class="faq-list">
          <details class="faq-item">
            <summary>¿Cómo usar {category.name} en mi rutina?</summary>
            <p>El orden de aplicación en la rutina coreana es: limpieza, tónico, sérum, hidratante, protector solar (mañana) o tratamiento (noche).</p>
          </details>
          <details class="faq-item">
            <summary>¿Cuánto tiempo tardaré en ver resultados?</summary>
            <p>Los productos de K-Beauty suelen mostrar resultados en 2-4 semanas de uso consistente. La clave es la paciencia y la aplicación diaria.</p>
          </details>
          <details class="faq-item">
            <summary>¿Son seguros para piel sensible?</summary>
            <p>Sí, la mayoría de los productos coreanos son formulados para piel sensible. Busca opciones sin fragancia y con certificación hipoalergénica.</p>
          </details>
          <details class="faq-item">
            <summary>¿Dónde comprar {category.name} en Colombia?</summary>
            <p>Puedes encontrar estos productos en tiendas online especializadas en K-Beauty que envíen a todo Colombia.</p>
          </details>
        </div>
      </section>
    </article>

    <aside class="sidebar">
      <h3>Otras Categorías</h3>
      <ul class="related-list">
        {relatedCategories.map(related => (
          <li>
            <a href={`/categoria/${related.slug}`}>{related.name}</a>
          </li>
        ))}
      </ul>
    </aside>
  </div>
</MainLayout>

<script type="application/ld+json" set:html={JSON.stringify(articleSchema)} />

<style>
  .container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 2rem 1rem;
  }
  
  .breadcrumb {
    display: flex;
    gap: 0.5rem;
    align-items: center;
    font-size: 0.9rem;
    color: var(--color-text-light);
    margin-bottom: 2rem;
  }
  
  .breadcrumb a {
    color: var(--color-text-light);
  }
  
  .breadcrumb a:hover {
    color: var(--color-primary);
  }
  
  .breadcrumb .current {
    color: var(--color-text);
    font-weight: 500;
  }
  
  .category-page {
    display: grid;
    grid-template-columns: 1fr 300px;
    gap: 2rem;
  }
  
  .category-header {
    grid-column: 1 / -1;
    margin-bottom: 2rem;
  }
  
  .category-header h1 {
    margin-bottom: 1rem;
  }
  
  .category-description {
    font-size: 1.2rem;
    color: var(--color-text-light);
    margin-bottom: 1.5rem;
  }
  
  .category-meta {
    display: flex;
    gap: 2rem;
  }
  
  .meta-label {
    display: block;
    font-size: 0.85rem;
    color: var(--color-text-light);
    margin-bottom: 0.5rem;
  }
  
  .meta-tags {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
  }
  
  .tag {
    background: var(--color-bg-secondary);
    padding: 0.25rem 0.75rem;
    border-radius: 20px;
    font-size: 0.85rem;
  }
  
  .content-section {
    margin-bottom: 3rem;
  }
  
  .content-section h2 {
    margin-bottom: 1rem;
  }
  
  .content-section h3 {
    margin: 1.5rem 0 0.75rem;
  }
  
  .content-section p {
    color: var(--color-text-light);
    margin-bottom: 1rem;
  }
  
  .content-section ul {
    margin-left: 1.5rem;
    color: var(--color-text-light);
  }
  
  .content-section li {
    margin-bottom: 0.5rem;
  }
  
  .benefits-list {
    list-style: none;
    margin-left: 0;
  }
  
  .benefits-list li {
    padding: 0.5rem 0;
    padding-left: 1.5rem;
    position: relative;
  }
  
  .benefits-list li::before {
    content: "✓";
    position: absolute;
    left: 0;
    color: var(--color-primary);
  }
  
  .products-grid {
    background: var(--color-bg-secondary);
    padding: 2rem;
    border-radius: 12px;
    text-align: center;
  }
  
  .coming-soon {
    color: var(--color-text-light);
  }
  
  .faq-section {
    margin-top: 3rem;
  }
  
  .faq-list {
    margin-top: 1.5rem;
  }
  
  .faq-item {
    background: var(--color-bg-secondary);
    border-radius: 8px;
    margin-bottom: 1rem;
    overflow: hidden;
  }
  
  .faq-item summary {
    padding: 1rem;
    cursor: pointer;
    font-weight: 500;
    list-style: none;
  }
  
  .faq-item summary::-webkit-details-marker {
    display: none;
  }
  
  .faq-item summary::after {
    content: "+";
    float: right;
    font-weight: bold;
  }
  
  .faq-item[open] summary::after {
    content: "-";
  }
  
  .faq-item p {
    padding: 0 1rem 1rem;
    color: var(--color-text-light);
  }
  
  .sidebar {
    background: var(--color-bg-secondary);
    padding: 1.5rem;
    border-radius: 12px;
    height: fit-content;
    position: sticky;
    top: 100px;
  }
  
  .sidebar h3 {
    margin-bottom: 1rem;
    font-size: 1.1rem;
  }
  
  .related-list {
    list-style: none;
  }
  
  .related-list li {
    margin-bottom: 0.75rem;
  }
  
  .related-list a {
    color: var(--color-text-light);
  }
  
  .related-list a:hover {
    color: var(--color-primary);
  }
  
  @media (max-width: 768px) {
    .category-page {
      grid-template-columns: 1fr;
    }
    
    .sidebar {
      position: static;
    }
  }
</style>
